# 2026년 04월 21일 기술 작업 상세 내역

## 1. 히스토리 그룹화 및 데이터 유실 문제 해결
### [문제점]
- 입고/출고/반납 등 동일한 대여 건에 대해 시트 상에 여러 행(Log)이 쌓일 때, UI에서 이를 개별 항목으로 노출하여 데이터가 중복되어 보임.
- 특히 반납 시점의 행에는 '파트너명' 정보가 누락된 경우가 많아, 단순하게 최신 행만 가져올 경우 파트너 정보가 빈칸으로 노출되는 버그 발생.

### [해결 방법]
- **Composite Key 그룹화**: `대여담당자`, `시작일`, `비고`를 조합한 키를 사용하여 `groups` 객체로 병합 처리.
- **Field Merging**: 그룹화 과정에서 `Object.keys(item).forEach`를 순회하며, 기존 그룹 객체에 비어있는 필드가 있고 현재 행에 데이터가 있다면 즉시 채워넣는 '로직상 보완'을 수행하여 파트너명 유실을 원천 차단.
- **최종 상태 감지**: `allStatuses` 배열을 유지하여 그룹 내 하나라도 `반납완료`가 존재하면 전체 건을 완료로 표시.

---

## 2. 컬럼 매핑 및 데이터 노이즈 필터링
### [문제점]
- 구글 시트의 컬럼 순서나 헤더 이름이 유동적이어서 고정된 키값으로는 '파트너명(Column I)'이나 '비고(Column O)'를 정확히 가져오지 못함.
- `Object.keys()`의 인덱스로 접근할 경우, 시트 내의 자동 생성 타임스탬프(`2026-03-05T...`)가 파트너명으로 오매핑되는 현상 발생.

### [해결 방법]
- **Fallback Keys**: `['파트너명', '파트너사명', 'partnerName', '사용자명' ...]` 등 가능한 모든 유사 키 리스트를 정의하고 `find`를 통해 동적 매핑.
- **RegExp 필터링**: 추출된 값이 날짜 형식(`YYYY-MM-DD`)을 가졌을 경우 파트너 정보가 아닌 노이즈로 간주하고 제외하는 정규식 필터 필터링 로직 도입.

---

## 3. 정렬 순서 및 3개 제한(Slice) 논리 오류
### [문제점]
- 사용자는 "최신 데이터가 아래로 쌓이는" 엑셀의 직관을 원했으나, 초기 코드는 단순히 날짜 내림차순 정렬 후 `slice(0, 3)`을 수행함.
- 날짜가 같은 경우 시트 상의 선후 관계를 구분하지 못해 순서가 꼬이는 불안정성 존재.

### [해결 방법]
- **Original Index 추적**: 서버 데이터를 map으로 순회할 때 `originalIndex` 속성을 부여하여 엑셀의 절대적 위치 보존.
- **정렬 기준 확립**: 1차 `dateA - dateB` (오름차순), 2차 `indexA - indexB` (동일 날짜 내 최신순 보장).
- **Slice 로직 수정**: 오름차순 리스트에서 '최신 3개'를 가져오기 위해 `slice(0, 3)`(고대 유물 3개) 대신 `slice(-3)`(최신 광물 3개)으로 변경하여 UI 요구사항 완벽 충족.

---

## 4. 빌드 안정성 확보
### [문제점]
- Vercel 배포 시 `unused-vars` (선언 후 미사용 변수) 린트 에러로 인해 빌드가 중단되는 현상(`columnIKey`).

### [해결 방법]
- 코드를 리팩토링하여 인덱스 기반의 불안정한 변수 선언을 제거하고, 명시적인 키 체크 로직으로 교체하여 빌드 환경에서의 CI 통과 보장.
