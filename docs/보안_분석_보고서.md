# 🔒 서버 보안 분석 보고서

## 📊 현재 보안 상태: ⚠️ 중간 위험

---

## 🚨 주요 보안 취약점

### 1. **HTTP만 사용 (HTTPS 없음)** - 🔴 높은 위험
- **문제**: 모든 통신이 암호화되지 않음
- **위험도**: 높음
- **영향**: 
  - 중간자 공격 (MITM) 가능
  - 데이터 가로채기 가능
  - 로그인 정보 유출 가능

### 2. **인증 없음** - 🔴 높은 위험
- **문제**: 누구나 `/api/refresh` 엔드포인트 호출 가능
- **위험도**: 높음
- **영향**:
  - 무제한 API 호출 가능
  - 서버 리소스 고갈
  - ERP 서버에 부하 발생

### 3. **Rate Limiting 없음** - 🟡 중간 위험
- **문제**: 요청 횟수 제한 없음
- **위험도**: 중간
- **영향**:
  - DDoS 공격 취약
  - 서버 다운 가능

### 4. **CORS 허용 범위 과도** - 🟡 중간 위험
- **문제**: `Access-Control-Allow-Origin: *` (모든 도메인 허용)
- **위험도**: 중간
- **영향**:
  - 악성 웹사이트에서 API 호출 가능
  - CSRF 공격 가능

### 5. **SSH 포트 공개** - 🟡 중간 위험
- **문제**: 포트 22가 인터넷에 공개됨
- **위험도**: 중간
- **영향**:
  - SSH 브루트포스 공격 가능
  - (다행히 키 기반 인증 사용 중)

### 6. **로그 모니터링 부족** - 🟢 낮은 위험
- **문제**: 공격 시도 감지 어려움
- **위험도**: 낮음
- **영향**: 공격 발생 시 늦게 발견

### 7. **입력 검증 부족** - 🟢 낮은 위험
- **문제**: API 엔드포인트에 입력 검증 없음
- **위험도**: 낮음 (현재는 단순 조회만)
- **영향**: 향후 기능 추가 시 취약점 가능

---

## 🎭 해킹 시나리오

### 시나리오 1: DDoS 공격 (가장 가능성 높음) ⚠️
**공격 방법:**
```bash
# 해커가 스크립트로 무한 요청
while true; do
  curl http://13.124.59.8:18273/api/stock
done
```

**결과:**
- 서버 CPU/메모리 고갈
- 서비스 다운
- 다른 사용자 접근 불가

**발생 확률:** 🔴 높음 (30-40%)
- 이유: 공개 IP, 인증 없음, Rate Limiting 없음

---

### 시나리오 2: API 남용 공격 ⚠️
**공격 방법:**
```bash
# /api/refresh 엔드포인트 무한 호출
for i in {1..1000}; do
  curl -X POST http://13.124.59.8:18273/api/refresh
done
```

**결과:**
- ERP 서버에 과도한 요청
- ERP 서버에서 IP 차단 가능
- 서버 리소스 고갈

**발생 확률:** 🟡 중간 (20-30%)
- 이유: 인증 없음, Rate Limiting 없음

---

### 시나리오 3: SSH 브루트포스 공격 🟡
**공격 방법:**
```bash
# 해커가 SSH 비밀번호 무작위 시도
hydra -l ubuntu -P passwords.txt ssh://13.124.59.8
```

**결과:**
- 키 기반 인증 사용 중이므로 **실패할 가능성 높음**
- 하지만 로그에 공격 시도 기록됨
- 서버 리소스 일부 소모

**발생 확률:** 🟡 중간 (10-20%)
- 이유: 공개 IP, 포트 22 공개
- **다행히**: 키 기반 인증으로 실제 침입은 어려움

---

### 시나리오 4: 중간자 공격 (MITM) 🔴
**공격 방법:**
- 해커가 같은 네트워크에 있거나 공용 Wi-Fi에서
- HTTP 트래픽 가로채기
- 데이터 변조 또는 유출

**결과:**
- 재고 데이터 유출
- 민감한 정보 노출

**발생 확률:** 🟡 중간 (15-25%)
- 이유: HTTP만 사용, HTTPS 없음

---

### 시나리오 5: 데이터 스크래핑 🤖
**공격 방법:**
```python
# 봇이 주기적으로 데이터 수집
import requests
import time

while True:
    data = requests.get('http://13.124.59.8:18273/api/stock').json()
    # 데이터 저장
    time.sleep(60)
```

**결과:**
- 재고 데이터 무단 수집
- 경쟁사 정보 수집 가능

**발생 확률:** 🟢 낮음 (5-10%)
- 이유: 공개 API지만 특별한 동기 부족

---

### 시나리오 6: 서버 침입 (가장 위험하지만 어려움) 🔴
**공격 방법:**
1. 서버 취약점 탐색
2. Python 서버 취약점 악용
3. 시스템 파일 접근 시도

**결과:**
- 서버 완전 장악
- 데이터 삭제/변조
- 다른 서버 공격의 발판

**발생 확률:** 🟢 낮음 (1-5%)
- 이유: 
  - 최신 Ubuntu 사용
  - 단순한 Python 서버 (취약점 적음)
  - 키 기반 SSH 인증

---

## 📈 위험도 종합 평가

| 항목 | 위험도 | 발생 확률 | 영향도 |
|------|--------|-----------|--------|
| DDoS 공격 | 🔴 높음 | 30-40% | 높음 |
| API 남용 | 🟡 중간 | 20-30% | 중간 |
| SSH 브루트포스 | 🟡 중간 | 10-20% | 낮음 |
| MITM 공격 | 🟡 중간 | 15-25% | 높음 |
| 데이터 스크래핑 | 🟢 낮음 | 5-10% | 낮음 |
| 서버 침입 | 🔴 높음 | 1-5% | 매우 높음 |

**종합 위험도: 🟡 중간-높음**

---

## 🛡️ 보안 개선 권장사항

### 즉시 적용 (높은 우선순위)

1. **Rate Limiting 추가** ⭐⭐⭐
   - IP당 분당 최대 60회 요청 제한
   - 구현 난이도: 낮음

2. **API 인증 추가** ⭐⭐⭐
   - `/api/refresh`에 API 키 또는 토큰 인증
   - 구현 난이도: 중간

3. **HTTPS 적용** ⭐⭐
   - Let's Encrypt 무료 SSL 인증서
   - 구현 난이도: 중간

### 중기 개선 (중간 우선순위)

4. **CORS 제한**
   - 특정 도메인만 허용
   - 구현 난이도: 낮음

5. **로그 모니터링**
   - fail2ban 설치 (SSH 공격 차단)
   - 구현 난이도: 낮음

6. **방화벽 강화**
   - 불필요한 포트 차단
   - 구현 난이도: 낮음

### 장기 개선 (낮은 우선순위)

7. **WAF (Web Application Firewall)**
   - Cloudflare 등 사용
   - 구현 난이도: 중간

8. **정기 보안 업데이트**
   - 자동 업데이트 설정
   - 구현 난이도: 낮음

---

## 💡 결론

**현재 상태:**
- ✅ 기본적인 보안은 유지됨 (키 기반 SSH, 최신 OS)
- ⚠️ 웹 서버 보안 취약점 다수
- 🔴 공개 서비스로서 위험 노출

**권장 조치:**
1. **Rate Limiting 즉시 추가** (가장 중요)
2. **API 인증 추가** (2순위)
3. **HTTPS 적용** (3순위)

**현실적인 평가:**
- 재고 조회 시스템이므로 **민감한 금융 정보는 없음**
- 하지만 **서비스 안정성**이 중요
- **DDoS 공격**에 가장 취약하므로 Rate Limiting 필수

**해커가 실제로 침입할 확률: 낮음 (1-5%)**
- 하지만 **서비스 방해 공격**은 높음 (30-40%)

---

## 📝 추가 참고사항

- AWS Lightsail은 기본적으로 DDoS 보호 제공 (제한적)
- 포트 18273은 비표준 포트라 스캔 확률 낮음
- 하지만 공개 IP이므로 발견되면 공격 가능

**현재로서는 "읽기 전용" 서비스이므로 큰 피해는 없을 것으로 예상되지만, 서비스 안정성을 위해 Rate Limiting은 필수입니다.**

